# Launcher test

If you're seeing this rendered (not raw markdown), `mdviewer.cmd` is wired up correctly.

```js
console.log("hello from the launcher");
```

```code
var i:int = 0;
```

```bash
uv sync
```

- Drop another file on this window to swap content
- Or close it and right-click any .md → **Open with → Markdown Viewer**
